# Base URL of the backend server — used by axios in src/context/context.jsx
# Must be prefixed with VITE_ to be exposed to the Vite client build.
# For local development this points at the Express server (default port 3001).
VITE_SERVER_URL=http://localhost:3001
